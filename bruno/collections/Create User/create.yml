info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3333/users
  body:
    type: json
    data: |-
      {
        "name": "Teste 02",
        "email": "test2@example.test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
