info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3333/meals
  body:
    type: json
    data: |-
      {
        "name": "Test",
        "description": "Test",
        "isOnDiet": true,
        "date": "2026-05-08"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
